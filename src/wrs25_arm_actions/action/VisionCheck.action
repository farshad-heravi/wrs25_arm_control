# Goal: Vision check request
string camera_id          # ID of the camera/vision module
string query              # Query type (e.g., "check_bottle", "check_box_closed", "check_container")
---
# Result: Vision check response
bool success              # True if object found/condition met, False otherwise
string message            # Result message
geometry_msgs/Pose target_pose  # Pose of detected object (only valid when success=true)
---
# Feedback: Processing status
string status             # Current processing status

